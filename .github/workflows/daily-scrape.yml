name: daily scrape

on:
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-scrape
  cancel-in-progress: false

jobs:
  scrape:
    # Self-hosted Linux runner. GitHub-hosted runners are not used because
    # the source site challenges their IP ranges.
    runs-on: [self-hosted, linux, x64, hotmap]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      # System Python 3. Persist venv in $HOME/.cache so deps install once
      # and reuse across runs.
      - name: Install dependencies (cached venv)
        run: |
          set -e
          VENV="$HOME/.cache/ph-heatmap-venv"
          if [ ! -d "$VENV" ]; then
            python3 -m venv "$VENV"
            "$VENV/bin/pip" install --upgrade -q pip
          fi
          "$VENV/bin/pip" install -q -r requirements.txt
          echo "VENV=$VENV" >> "$GITHUB_ENV"

      - name: Run scraper
        env:
          PYTHONUNBUFFERED: "1"
          PH_IMPERSONATE: ${{ vars.PH_IMPERSONATE || 'chrome120' }}
        run: |
          "$VENV/bin/python" run.py

      - name: Commit and push artifacts
        run: |
          git config user.name "hotmap-bot"
          git config user.email "hotmap-bot@users.noreply.github.com"
          git add data.db public/
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "chore(data): daily snapshot $(date -u +%Y-%m-%d)"
          git pull --rebase origin main
          git push
